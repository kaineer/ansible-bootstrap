- name: "Install {{ item.name }}"
  apt:
    name: "{{ item.name }}"
    state: present
  loop:
    - { name: git }
    - { name: tmux }
    - { name: nvim }
    - { name: jq }
    - { name: rustc }
    - { name: cargo }
